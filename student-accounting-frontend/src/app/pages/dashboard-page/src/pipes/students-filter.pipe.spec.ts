import { StudentsFilterPipe } from './students-filter.pipe';

describe('StudentsFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new StudentsFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
